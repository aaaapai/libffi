name: LWJGL Build

on:
  push:
    branches:
      - master

env:
  AWS_DEFAULT_REGION: us-east-1
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  S3_PARAMS: --cache-control "public,must-revalidate,proxy-revalidate,max-age=0"

jobs:
  linux:
    name: Linux
    runs-on: ubuntu-latest
    container:
      image: almalinux:8
    strategy:
      fail-fast: false
      matrix:
        ARCH: [x64]
        include:
          - ARCH: x64
            HOST: x86_64-pc-linux-gnu
    defaults:
      run:
        shell: bash
    steps:
      - name: Install git
        run: dnf -y install git
      - name: Clone repository
        run: git clone --depth 3 https://github.com/${{ github.repository }}.git .
      - name: Install build dependencies
        run: |
          dnf -y install epel-release wget
          wget https://pkgs.sysadmins.ws/el8/base/x86_64/raven-release-1.0.1-1.el8.noarch.rpm
          rpm -ivh raven-release-1.0.1-1.el8.noarch.rpm
          dnf config-manager --set-enabled powertools
          dnf config-manager --set-enabled raven-extras
          dnf -y update
          dnf -y install gcc-toolset-15-gcc awscli
      - name: Install libffi dependencies
        run: dnf -y install autoconf automake autoconf libtool texinfo
      - name: Configure build
        run: |
          source /opt/rh/gcc-toolset-15/enable || true
          ./autogen.sh
          ./configure --disable-shared --with-pic --host=${{matrix.HOST}} || (cat */config.log ; exit 1)
      - name: Build
        run: |
          source /opt/rh/gcc-toolset-15/enable || true
          make
      - name: Upload artifact
        run: aws s3 cp ${{matrix.HOST}}/.libs/libffi_convenience.a s3://lwjgl-build/nightly/linux/${{matrix.ARCH}}/core/libffi.a ${{env.S3_PARAMS}}

  linux-cross:
    name: Linux Cross
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        ARCH: [arm32, arm64, mips64, ppc64le, riscv64]
        include:
          # -----
          - ARCH: arm32
            CROSS_ARCH: armhf
            TRIPLET: arm-linux-gnueabihf
            HOST: arm-unknown-linux-gnueabihf
          # -----
          - ARCH: arm64
            CROSS_ARCH: arm64
            TRIPLET: aarch64-linux-gnu
            HOST: aarch64-unknown-linux-gnu
          # ----
          - ARCH: ppc64le
            CROSS_ARCH: ppc64el
            TRIPLET: powerpc64le-linux-gnu
            HOST: powerpc64le-unknown-linux-gnu
          # -----
          - ARCH: riscv64
            CROSS_ARCH: riscv64
            TRIPLET: riscv64-linux-gnu
            HOST: riscv64-unknown-linux-gnu
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v6
        with
          fetch-depth: 1
      - name: Install dependencies
        run: |
          DEBIAN_FRONTEND=noninteractive sudo apt-get -yq update
          DEBIAN_FRONTEND=noninteractive sudo apt-get -yq install m4 make automake libtool texinfo \
            gcc-12-${{matrix.TRIPLET}} \
            libc6-dev-${{matrix.CROSS_ARCH}}-cross
      - name: Install autoconf 2.72
        run: |
          curl -L https://ftp.gnu.org/gnu/autoconf/autoconf-2.72.tar.gz | tar -x
          (cd autoconf-2.72; ./configure; make; sudo make install)
      - name: Configure build
        run: |
          PATH=$HOME/i/bin:$PATH ./autogen.sh
          ./configure --disable-shared --with-pic CC=${{matrix.TRIPLET}}-gcc-12 --host=${{matrix.HOST}} || (cat */config.log ; exit 1)
      - name: Build
        run: make
      - name: Upload artifact
        run: aws s3 cp ${{matrix.HOST}}/.libs/libffi_convenience.a s3://lwjgl-build/nightly/linux/${{matrix.ARCH}}/core/libffi.a ${{env.S3_PARAMS}}
  freebsd-cross:
    name: FreeBSD Cross
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - name: Buil
        uses: cross-platform-actions/action@v0.32.0
        with:
          operating_system: freebsd
          architecture: x86-64
          version: '13.5'
          memory: 4G
          shell: bash
          run: |
            sudo pkg install -y m4 gmake automake libtool texinfo
            ./autogen.s
            ./configure --disable-shared --with-pic || (cat */config.log ; exit 1)
            make
      - name: Upload artifact
        run: aws s3 cp x86_64-unknown-freebsd13.5/.libs/libffi_convenience.a s3://lwjgl-build/nightly/freebsd/x64/core/libffi.a ${{env.S3_PARAMS}}

  macos:
    name: macOS
    runs-on: macos-latest
    strategy:
      fail-fast: fals
      matrix:
        ARCH: [x64, arm64]
        include:
          - ARCH: x64
            DARWIN: 15
            CC: MACOSX_DEPLOYMENT_TARGET=10.11
            CFLAGS: -target x64-apple-darwin -arch x86_64 -mmacosx-version-min=10.11
            LDFLAGS: -target x64-apple-darwin -arch x86_64 -mmacosx-version-min=10.11
            HOST: x86_64
          - ARCH: arm6
            DARWIN: 20
            CC: MACOSX_DEPLOYMENT_TARGET=11.0
            CFLAGS: -target aarch64-apple-darwin -arch arm64 -mmacosx-version-min=11.0
            LDFLAGS: -target aarch64-apple-darwin -arch arm64 -mmacosx-version-min=11.0
            HOST: aarch64
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
      - name: Install dependencie
        run: brew install automake libtool texinfo
      - name: Configure build
        run: |
          ./autogen.sh
          ${{matrix.CC}} ./configure --disable-shared CC=clang CFLAGS='${{matrix.CFLAGS}}' LDFLAGS='${{matrix.LDFLAGS}}' --target ${{matrix.ARCH}}-apple-darwin${{matrix.DARWIN}} --host=${{matrix.HOST}}-apple-darwin${{matrix.DARWIN}} || (cat */config.log ; exit 1)
      - name: Build
        run: ${{matrix.CC}} make all
      - name: Upload artifact
        run: aws s3 cp ${{matrix.HOST}}-apple-darwin${{matrix.DARWIN}}/.libs/libffi_convenience.a s3://lwjgl-build/nightly/macosx/${{matrix.ARCH}}/core/libffi.a ${{env.S3_PARAMS}}
  windows:
    name: Windows
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        ARCH: [x86, x64, arm64]
        include:
          # -----
          - ARCH: x8
            SOURCE_ARCH: x86
            VCTOOLS: x86.x64
            #MSVC_ARCH: amd64_x86
            MSVC_ARCH: x86
            MSVC_SH: -m32
            HOST: i686-pc-cygwin
          # -----
          - ARCH: x64
            SOURCE_ARCH: x86
            VCTOOLS: x86.x6
            MSVC_ARCH: amd64
            MSVC_SH: -m64
            HOST: x86_64-pc-cygwin
          # -----
          - ARCH: arm64
            SOURCE_ARCH: aarch64
            MSVC_ARCH: amd64_arm64
            MSVC_SH: -marm64
            HOST: aarch64-pc-cygwin
    defaults:
      run:
        shell: cmd
    steps:
      - uses: actions/checkout@v6

          call "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\VsDevCmd.bat" -host_arch=amd64 -arch=${{matrix.MSVC_ARCH}}
          cl.exe
          C:\cygwin\bin\bash.exe --login --norc -eo pipefail -o igncr build.sh
        shell: C:\cygwin\bin\bash.exe --login --norc -eo pipefail -o igncr '{0}'
      - name: Upload artifact
        run: aws s3 cp ${{matrix.HOST}}\.libs\libffi_convenience.lib s3://lwjgl-build/nightly/windows/${{matrix.ARCH}}/core/l